import React from 'react';

export default class FavoritesView extends React.Component {
        constructor() {
            super();
            this.state = {};
        }

        render() {
            return ( < div className = 'five wide column' > testing favorites view < /div>)
            }
        }